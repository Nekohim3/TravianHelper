<Page x:Class="TravianHelper.UI.NewPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TravianHelper.UI"
      xmlns:converters="clr-namespace:TravianHelper.Converters"
      mc:Ignorable="d" d:DataContext="{d:DesignInstance local:NewPageViewModel}"
      d:DesignHeight="700" d:DesignWidth="1600"
      Title="NewPageView">
    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:BoolToVisibilityReverseConverter x:Key="BoolToVisibilityReverseConverter"/>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <GroupBox Header="Прокси" DataContext="{Binding ProxySettingsVM}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <ListView ItemsSource="{Binding ProxyList}" SelectedItem="{Binding SelectedProxy}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Grid Margin="5,1,5,1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border x:Name="BorderForSelect" Background="#553355cc"  Visibility="Collapsed" Margin="-5,0,-5,0"/>
                                                <TextBlock Grid.Column="0" Text="{Binding }" Margin="0" VerticalAlignment="Center" Width="136"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="true">
                                                    <Setter TargetName="BorderForSelect" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                    <Grid Grid.Column="1" Margin="5,0,5,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Используется в аккаунтах:" Margin="3"/>
                        <ListView Grid.Row="1" ItemsSource="{Binding UsedAccountList}">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Grid Margin="5">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding }" Margin="0" VerticalAlignment="Center"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>
                    <Grid Grid.Column="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="IP" Grid.Row="0" Grid.Column="0" Margin="1"/>
                        <TextBlock Text="Порт" Grid.Row="1" Grid.Column="0" Margin="1"/>
                        <TextBlock Text="Логин" Grid.Row="2" Grid.Column="0" Margin="1"/>
                        <TextBlock Text="Пароль" Grid.Row="3" Grid.Column="0" Margin="1"/>
                        <TextBox IsEnabled="{Binding IsEditMode}" Text="{Binding CurrentProxy.Ip}" Grid.Row="0" Grid.Column="1" Margin="1"/>
                        <TextBox IsEnabled="{Binding IsEditMode}" Text="{Binding CurrentProxy.Port}" Grid.Row="1" Grid.Column="1" Margin="1"/>
                        <TextBox IsEnabled="{Binding IsEditMode}" Text="{Binding CurrentProxy.UserName}" Grid.Row="2" Grid.Column="1" Margin="1"/>
                        <TextBox IsEnabled="{Binding IsEditMode}" Text="{Binding CurrentProxy.Password}" Grid.Row="3" Grid.Column="1" Margin="1"/>

                        <Grid Grid.Column="0" Grid.Row="5" ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Command="{Binding AddCmd}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityReverseConverter}}" Content="Добавить" Grid.Column="0" Margin="3"/>
                            <Button Command="{Binding EditCmd}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityReverseConverter}}" Content="Редактировать" Grid.Column="1" Margin="3"/>
                            <Button Command="{Binding SaveCmd}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}" Content="Сохранить" Grid.Column="0" Margin="3"/>
                            <Button Command="{Binding CancelCmd}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}" Content="Отмена" Grid.Column="1" Margin="3"/>

                        </Grid>


                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>
        <Grid Grid.Column="1">

        </Grid>
    </Grid>
</Page>
